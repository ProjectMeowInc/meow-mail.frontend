name: Spellcheck
on: push
jobs:
  build:
    name: Spellcheck
      runs-on: ubuntu-latest
      steps:
        - uses: taiki-e/checkout-action@v1

        - uses: crate-ci/typos@master
          with:
            files: .put path to configuration file here
